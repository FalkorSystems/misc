[header]
	name Robot
	version 0000
	manufacturer Falkor Systems
	part RobotBone
	serial 0000

[current]
	3.3vdd 123mA
	5vdd 500mA
	5sys 2000mA
	supplied 1234mA

# UART1 for PWM Input
[pin p9-26] # Rx
     in
     fast
     rx
     pull down
     pull enable
     mux uart1_rxd

[pin p9-24] # Tx
     out
     fast
     pull down
     pull enable
     mux uart1_txd

# UART2 for GPS
[pin p9-22] # Rx
     in
     fast
     rx
     pull down
     pull enable
     mux uart2_rxd

[pin p9-21] # Tx
     out
     fast
     pull down
     pull enable
     mux uart2_txd

# UART4 for XBee
[pin p9-11] # Rx
     in
     fast
     rx
     pull down
     pull enable
     mux uart4_rxd

[pin p9-13] # Tx
     out
     fast
     pull down
     pull enable
     mux uart4_txd

# UART5 for Servo Controller
[pin p8-31] # Rx
     in
     fast
     rx
     pull down
     pull enable
     mux uart5_rxd

[pin p8-37] # Tx
     out
     fast
     pull down
     pull enable
     mux uart5_txd

# I2C for IMU and Barometer
[pin p9-17]
     bidir
     fast
     mux i2c1_scl

[pin p9-18]
     bidir
     fast
     mux i2c1_sda

#
# No muxing required for analog
#
